import { getRedis } from './_kv.mjs'
export default async function handler(req,res){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Content-Type','application/json');const r=getRedis();const key='players';if(req.method==='GET')return res.json((await r.get(key))||{singles:[],doubles:[]});if(req.method==='POST'){const b=JSON.parse(req.body||'{}');await r.set(key,b.payload);return res.json({ok:true})}}